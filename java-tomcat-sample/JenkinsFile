pipeline {
    agent any
    stages {
        stage('Build application') {
            steps {
                sh "mvn java-tomcat-sample/pom.xml clean package"
            }
            post {
                sucesss {
                    echo "Archiving artiffcats"
                    archiveArtifacts artifacts: '**/*.war'
                }
            }
        }
    }
}